# note-domain 模块最小任务

- [ ] 定义 `Note` 类型与基础字段校验（id/title/content/tags/color/pinned/timestamps/deleted）。
- [ ] 实现最小 CRUD 用例：创建、编辑、软删除。
- [ ] 实现标签操作：添加标签、移除标签、按标签过滤。
- [ ] 实现置顶/颜色更新与排序规则（置顶优先，次级 updatedAt）。
- [ ] 增加单元测试：CRUD、标签过滤、排序规则。
