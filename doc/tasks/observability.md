# observability 模块最小任务

- [ ] 定义结构化日志字段规范（禁止敏感字段）。
- [ ] 增加关键计数指标：push/pull 请求数、解密失败数、冲突处理数。
- [ ] 实现 `/healthz` 依赖检查（SQLite 可用性）。
- [ ] 为关键路径打点：同步周期、失败重试、队列长度。
- [ ] 增加测试：数据库不可用时 healthz 状态与日志输出符合预期。
