### 项目浮想：加密、离线优先的在线便笺 PWA —— 你的多语言 Vibe 编程练手场

**项目简介**
你将构建一个可以自托管、可安装的 PWA 在线便笺，彻底替代随手撕下的便条和散落各处的笔记应用。它拥有精美、响应式的便签墙，默认离线可用，所有数据在浏览器中加密存储，并可通过你自己的 Ubuntu Docker 服务器进行多设备同步。通过 Chrome 的“保存为应用”安装后，它就像一个原生的桌面软件。这个项目看似界面简约，实则功能丰盈，背后蕴含着一个全栈实践课题，几乎能触达一门新语言的所有核心概念。

**为什么它独特且相对冷门**
大多数笔记应用要么是臃肿的 SaaS 服务，要么是本地 Markdown 文件。而它是你专属的、自托管的、零信任的私人空间：笔记在离开浏览器前就已加密，即使你是服务器管理员也无法窥视。结合离线优先架构（Service Worker + IndexedDB）和极简、无干扰的 UI，它会成为你爱不释手的日常伴侣。市面上几乎没有现成方案能同时满足隐私优先、自托管且拥有 PWA 形态的需求。

**核心模块（在实践中学习）**
当你一步步实现它时，将系统性地探索这门语言的全貌：

1. **HTTP 服务器与路由**  
   - 提供静态资源（HTML、JS、CSS）并实现同步用的 JSON API。学习语言的 HTTP 库、中间件模式及路由分组。

2. **数据持久化与加密逻辑**  
   - 后端将加密的二进制数据存入 SQLite 或文件系统。实现密钥派生（PBKDF2/Argon2）和 AES-GCM 加解密——但所有密码学操作实际发生在前端，服务器只看到不透明密文。

3. **并发 / 异步**  
   - 同时处理多个同步请求而不阻塞。体会协程、async/await 或通道等语言核心特性。

4. **文件读写与配置管理**  
   - 读取配置文件设定端口、数据库路径和同步参数。学习文件操作、环境变量及语言惯用的配置格式。

5. **静态资源嵌入**  
   - 将整个前端（HTML、CSS、JavaScript、manifest、Service Worker）打包进编译后的二进制文件。掌握资源嵌入、文件系统抽象与单文件部署。

6. **PWA 与离线优先前端（用 JavaScript 编写，但结构由后端语言决定）**  
   - 尽管前端采用原生 JS，但如何提供和组织这些文件反映了后端语言处理静态内容、缓存头和内容类型的方式。

7. **Docker 容器化**  
   - 编写多阶段 Dockerfile，构建二进制文件并在 Ubuntu 上运行。理解容器化、端口映射与卷挂载——这些技能在任何语言中皆可迁移。

8. **测试与优雅降级**  
   - 为同步 API 编写单元测试，模拟加密过程，模拟网络故障。学习语言的测试惯用法。实现优雅关闭和健康检查。

**成就感解锁**
当你打开那个钉在 Windows 任务栏上的、干净优雅的自托管便笺时，会涌起巨大的拥有感。它不只是一个工具；它是你的每日日志、任务板和灵感收纳器——完全由你亲手打造，用你正在掌握的那门语言。

---

### 推荐编程语言与工具链

**推荐语言：Go**  
Go 天生适合构建这种“单二进制、自托管、高并发”的服务。其标准库强大到足以编写完整的 API 服务器和静态资源服务，同时轻量的 goroutine 让并发同步变得异常简单。你将深入体会 Go 的接口、错误处理、结构体标记、`embed` 嵌入等独特特性，而这正是把一门语言学透所必需的。

**工具链：**  
- **Go 1.21+** 与 `go mod`  
- **标准库优先**：`net/http`、`encoding/json`、`crypto/aes`、`database/sql`、`embed`  
- **轻量外部依赖**：仅推荐 `modernc.org/sqlite`（纯 Go 实现的 SQLite 驱动，无需 CGO）来体验外部包管理  
- **前端**：使用原生 HTML、CSS、JavaScript 编写 PWA（Service Worker、IndexedDB、Web Crypto API），Go 通过 `//go:embed` 将前端文件嵌入二进制  
- **容器化**：Docker 多阶段构建，生成极小镜像，部署于 Ubuntu 22.04  
